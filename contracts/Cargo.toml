[workspace]
members = [
    "programs/group_manager",
    "programs/expense_splitter",
    "programs/settlement_engine",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
overflow-checks = false
